// Disabled for JMT Drive Solutions — no demo data. Real employees are invited via
// /admin/employees once the first admin is bootstrapped.

console.log("Demo seed disabled — JMT Drive Solutions starts with empty data.");
process.exit(0);
